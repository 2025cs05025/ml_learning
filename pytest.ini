# pytest.ini — repository-root test configuration.
# pythonpath = src  → allows imports like ``from config.paths`` without env tweaks.
# Run from repo root: pytest tests/ -v

[pytest]
pythonpath = src
testpaths = tests
