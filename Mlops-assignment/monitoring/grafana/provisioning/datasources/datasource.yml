# Grafana datasource provisioning — Prometheus at http://prometheus:9090 (Compose network).
---
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
